# API Notes (Initial)

This repository currently prioritizes SSR pages and domain modeling. The `apps.api` app is reserved for versioned endpoints.

Planned initial endpoints:

- `GET /api/v1/entries/`
- `GET /api/v1/entries/{slug}/`
- `POST /api/v1/definitions/`
- `POST /api/v1/archive-records/`
